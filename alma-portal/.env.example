# Alma Team Portal — environment configuration
#
# LEAVE THESE BLANK to run in DEMO MODE (data lives in the browser, no backend).
# FILL THEM IN to switch the whole team onto real, shared, multi-tenant accounts.
#
# Get both values from your Supabase project:
#   Supabase dashboard  ->  Project Settings  ->  API
#
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
